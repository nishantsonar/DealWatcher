spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pricedropalertapp
    username: root
    password: password
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          format_sql: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: my-group
      auto-offset-reset: earliest
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer

com:
  scheduled:
    cron: '@midnight'  #  0 0 10 * * ?

email:
  username: nishantsonar9@gmail.com
  password: czvxcrkiubwkibvc